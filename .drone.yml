image: mischief/docker-golang
env:
  - GOPATH=/var/cache/drone
script:
  - go build
  - go test -v
  - go install
services:
  - redis
notify:
  email:
    recipients:
      - centurylinkcloud@gmail.com
